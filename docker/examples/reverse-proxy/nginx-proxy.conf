# =============================================
# NGINX REVERSE PROXY CONFIG
# =============================================
# Route domain ke Nginx masing-masing project
# =============================================

# =============================================
# WEB 1: smartagri.web.id → :8080
# =============================================
server {
    listen 80;
    listen [::]:80;
    server_name smartagri.web.id www.smartagri.web.id;

    location / {
        proxy_pass http://smartagri-nginx:80;    # ← Forward ke nginx web 1
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


# =============================================
# WEB 2: tokoonline.com → :8081
# =============================================
server {
    listen 80;
    listen [::]:80;
    server_name tokoonline.com www.tokoonline.com;

    location / {
        proxy_pass http://tokoonline-nginx:80;   # ← Forward ke nginx web 2
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


# =============================================
# WEB 3: portfolio.me → :8082
# =============================================
server {
    listen 80;
    listen [::]:80;
    server_name portfolio.me www.portfolio.me;

    location / {
        proxy_pass http://portfolio-nginx:80;    # ← Forward ke nginx web 3
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


# =============================================
# TAMBAH WEB BARU:
# Copy server block di atas, ganti:
# 1. server_name → domain baru
# 2. proxy_pass → container nginx baru
# =============================================
